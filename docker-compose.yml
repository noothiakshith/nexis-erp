services:
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_DATABASE: nexis_erp
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_USER: user
      MYSQL_PASSWORD: user_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
